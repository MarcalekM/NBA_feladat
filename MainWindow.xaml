<Window x:Class="NBA_feladat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NBA_feladat"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1200">
    <Grid>
        <ComboBox x:Name="Csapatok" HorizontalAlignment="Left" Margin="30,45,0,0" VerticalAlignment="Top" Width="150" SelectionChanged="Csapatok_SelectionChanged"/>
        <DataGrid x:Name="Jatekosok" Margin="800,27,50,277" Width="350" Height="280">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mezszám" Binding="{Binding Mezszam}"/>
                <DataGridTextColumn Header="Név" Binding="{Binding Nev}"/>
                <DataGridTextColumn Header="Poszt" Binding="{Binding Poszt}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
